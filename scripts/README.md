# Scripts

Store project helper scripts here.

Scripts may automate local setup, validation, maintenance, or release tasks. Keep primary application logic in `bookvoice/`.

Run `./scripts/check.sh` for the default local quality checks.
